// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"github.com/facebookincubator/ent/dialect/sql"
	"github.com/sut63/team17/app/ent/professorship"
)

// Professorship is the model entity for the Professorship schema.
type Professorship struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Professorship holds the value of the "professorship" field.
	Professorship string `json:"professorship,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ProfessorshipQuery when eager-loading is set.
	Edges ProfessorshipEdges `json:"edges"`
}

// ProfessorshipEdges holds the relations/edges for other nodes in the graph.
type ProfessorshipEdges struct {
	// ProsProf holds the value of the pros_prof edge.
	ProsProf []*Professor
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// ProsProfOrErr returns the ProsProf value or an error if the edge
// was not loaded in eager-loading.
func (e ProfessorshipEdges) ProsProfOrErr() ([]*Professor, error) {
	if e.loadedTypes[0] {
		return e.ProsProf, nil
	}
	return nil, &NotLoadedError{edge: "pros_prof"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Professorship) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{},  // id
		&sql.NullString{}, // professorship
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Professorship fields.
func (pr *Professorship) assignValues(values ...interface{}) error {
	if m, n := len(values), len(professorship.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	pr.ID = int(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field professorship", values[0])
	} else if value.Valid {
		pr.Professorship = value.String
	}
	return nil
}

// QueryProsProf queries the pros_prof edge of the Professorship.
func (pr *Professorship) QueryProsProf() *ProfessorQuery {
	return (&ProfessorshipClient{config: pr.config}).QueryProsProf(pr)
}

// Update returns a builder for updating this Professorship.
// Note that, you need to call Professorship.Unwrap() before calling this method, if this Professorship
// was returned from a transaction, and the transaction was committed or rolled back.
func (pr *Professorship) Update() *ProfessorshipUpdateOne {
	return (&ProfessorshipClient{config: pr.config}).UpdateOne(pr)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (pr *Professorship) Unwrap() *Professorship {
	tx, ok := pr.config.driver.(*txDriver)
	if !ok {
		panic("ent: Professorship is not a transactional entity")
	}
	pr.config.driver = tx.drv
	return pr
}

// String implements the fmt.Stringer.
func (pr *Professorship) String() string {
	var builder strings.Builder
	builder.WriteString("Professorship(")
	builder.WriteString(fmt.Sprintf("id=%v", pr.ID))
	builder.WriteString(", professorship=")
	builder.WriteString(pr.Professorship)
	builder.WriteByte(')')
	return builder.String()
}

// Professorships is a parsable slice of Professorship.
type Professorships []*Professorship

func (pr Professorships) config(cfg config) {
	for _i := range pr {
		pr[_i].config = cfg
	}
}
