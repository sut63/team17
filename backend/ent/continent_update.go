// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"

	"github.com/facebookincubator/ent/dialect/sql"
	"github.com/facebookincubator/ent/dialect/sql/sqlgraph"
	"github.com/facebookincubator/ent/schema/field"
	"github.com/sut63/team17/app/ent/continent"
	"github.com/sut63/team17/app/ent/predicate"
	"github.com/sut63/team17/app/ent/province"
)

// ContinentUpdate is the builder for updating Continent entities.
type ContinentUpdate struct {
	config
	hooks      []Hook
	mutation   *ContinentMutation
	predicates []predicate.Continent
}

// Where adds a new predicate for the builder.
func (cu *ContinentUpdate) Where(ps ...predicate.Continent) *ContinentUpdate {
	cu.predicates = append(cu.predicates, ps...)
	return cu
}

// SetContinent sets the continent field.
func (cu *ContinentUpdate) SetContinent(s string) *ContinentUpdate {
	cu.mutation.SetContinent(s)
	return cu
}

// AddContProvIDs adds the cont_prov edge to Province by ids.
func (cu *ContinentUpdate) AddContProvIDs(ids ...int) *ContinentUpdate {
	cu.mutation.AddContProvIDs(ids...)
	return cu
}

// AddContProv adds the cont_prov edges to Province.
func (cu *ContinentUpdate) AddContProv(p ...*Province) *ContinentUpdate {
	ids := make([]int, len(p))
	for i := range p {
		ids[i] = p[i].ID
	}
	return cu.AddContProvIDs(ids...)
}

// Mutation returns the ContinentMutation object of the builder.
func (cu *ContinentUpdate) Mutation() *ContinentMutation {
	return cu.mutation
}

// RemoveContProvIDs removes the cont_prov edge to Province by ids.
func (cu *ContinentUpdate) RemoveContProvIDs(ids ...int) *ContinentUpdate {
	cu.mutation.RemoveContProvIDs(ids...)
	return cu
}

// RemoveContProv removes cont_prov edges to Province.
func (cu *ContinentUpdate) RemoveContProv(p ...*Province) *ContinentUpdate {
	ids := make([]int, len(p))
	for i := range p {
		ids[i] = p[i].ID
	}
	return cu.RemoveContProvIDs(ids...)
}

// Save executes the query and returns the number of rows/vertices matched by this operation.
func (cu *ContinentUpdate) Save(ctx context.Context) (int, error) {
	if v, ok := cu.mutation.Continent(); ok {
		if err := continent.ContinentValidator(v); err != nil {
			return 0, &ValidationError{Name: "continent", err: fmt.Errorf("ent: validator failed for field \"continent\": %w", err)}
		}
	}

	var (
		err      error
		affected int
	)
	if len(cu.hooks) == 0 {
		affected, err = cu.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*ContinentMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			cu.mutation = mutation
			affected, err = cu.sqlSave(ctx)
			mutation.done = true
			return affected, err
		})
		for i := len(cu.hooks) - 1; i >= 0; i-- {
			mut = cu.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, cu.mutation); err != nil {
			return 0, err
		}
	}
	return affected, err
}

// SaveX is like Save, but panics if an error occurs.
func (cu *ContinentUpdate) SaveX(ctx context.Context) int {
	affected, err := cu.Save(ctx)
	if err != nil {
		panic(err)
	}
	return affected
}

// Exec executes the query.
func (cu *ContinentUpdate) Exec(ctx context.Context) error {
	_, err := cu.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (cu *ContinentUpdate) ExecX(ctx context.Context) {
	if err := cu.Exec(ctx); err != nil {
		panic(err)
	}
}

func (cu *ContinentUpdate) sqlSave(ctx context.Context) (n int, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   continent.Table,
			Columns: continent.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: continent.FieldID,
			},
		},
	}
	if ps := cu.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if value, ok := cu.mutation.Continent(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: continent.FieldContinent,
		})
	}
	if nodes := cu.mutation.RemovedContProvIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   continent.ContProvTable,
			Columns: []string{continent.ContProvColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: province.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Clear = append(_spec.Edges.Clear, edge)
	}
	if nodes := cu.mutation.ContProvIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   continent.ContProvTable,
			Columns: []string{continent.ContProvColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: province.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Add = append(_spec.Edges.Add, edge)
	}
	if n, err = sqlgraph.UpdateNodes(ctx, cu.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{continent.Label}
		} else if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return 0, err
	}
	return n, nil
}

// ContinentUpdateOne is the builder for updating a single Continent entity.
type ContinentUpdateOne struct {
	config
	hooks    []Hook
	mutation *ContinentMutation
}

// SetContinent sets the continent field.
func (cuo *ContinentUpdateOne) SetContinent(s string) *ContinentUpdateOne {
	cuo.mutation.SetContinent(s)
	return cuo
}

// AddContProvIDs adds the cont_prov edge to Province by ids.
func (cuo *ContinentUpdateOne) AddContProvIDs(ids ...int) *ContinentUpdateOne {
	cuo.mutation.AddContProvIDs(ids...)
	return cuo
}

// AddContProv adds the cont_prov edges to Province.
func (cuo *ContinentUpdateOne) AddContProv(p ...*Province) *ContinentUpdateOne {
	ids := make([]int, len(p))
	for i := range p {
		ids[i] = p[i].ID
	}
	return cuo.AddContProvIDs(ids...)
}

// Mutation returns the ContinentMutation object of the builder.
func (cuo *ContinentUpdateOne) Mutation() *ContinentMutation {
	return cuo.mutation
}

// RemoveContProvIDs removes the cont_prov edge to Province by ids.
func (cuo *ContinentUpdateOne) RemoveContProvIDs(ids ...int) *ContinentUpdateOne {
	cuo.mutation.RemoveContProvIDs(ids...)
	return cuo
}

// RemoveContProv removes cont_prov edges to Province.
func (cuo *ContinentUpdateOne) RemoveContProv(p ...*Province) *ContinentUpdateOne {
	ids := make([]int, len(p))
	for i := range p {
		ids[i] = p[i].ID
	}
	return cuo.RemoveContProvIDs(ids...)
}

// Save executes the query and returns the updated entity.
func (cuo *ContinentUpdateOne) Save(ctx context.Context) (*Continent, error) {
	if v, ok := cuo.mutation.Continent(); ok {
		if err := continent.ContinentValidator(v); err != nil {
			return nil, &ValidationError{Name: "continent", err: fmt.Errorf("ent: validator failed for field \"continent\": %w", err)}
		}
	}

	var (
		err  error
		node *Continent
	)
	if len(cuo.hooks) == 0 {
		node, err = cuo.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*ContinentMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			cuo.mutation = mutation
			node, err = cuo.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(cuo.hooks) - 1; i >= 0; i-- {
			mut = cuo.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, cuo.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX is like Save, but panics if an error occurs.
func (cuo *ContinentUpdateOne) SaveX(ctx context.Context) *Continent {
	c, err := cuo.Save(ctx)
	if err != nil {
		panic(err)
	}
	return c
}

// Exec executes the query on the entity.
func (cuo *ContinentUpdateOne) Exec(ctx context.Context) error {
	_, err := cuo.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (cuo *ContinentUpdateOne) ExecX(ctx context.Context) {
	if err := cuo.Exec(ctx); err != nil {
		panic(err)
	}
}

func (cuo *ContinentUpdateOne) sqlSave(ctx context.Context) (c *Continent, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   continent.Table,
			Columns: continent.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: continent.FieldID,
			},
		},
	}
	id, ok := cuo.mutation.ID()
	if !ok {
		return nil, &ValidationError{Name: "ID", err: fmt.Errorf("missing Continent.ID for update")}
	}
	_spec.Node.ID.Value = id
	if value, ok := cuo.mutation.Continent(); ok {
		_spec.Fields.Set = append(_spec.Fields.Set, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: continent.FieldContinent,
		})
	}
	if nodes := cuo.mutation.RemovedContProvIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   continent.ContProvTable,
			Columns: []string{continent.ContProvColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: province.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Clear = append(_spec.Edges.Clear, edge)
	}
	if nodes := cuo.mutation.ContProvIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   continent.ContProvTable,
			Columns: []string{continent.ContProvColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: province.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges.Add = append(_spec.Edges.Add, edge)
	}
	c = &Continent{config: cuo.config}
	_spec.Assign = c.assignValues
	_spec.ScanValues = c.scanValues()
	if err = sqlgraph.UpdateNode(ctx, cuo.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{continent.Label}
		} else if cerr, ok := isSQLConstraintError(err); ok {
			err = cerr
		}
		return nil, err
	}
	return c, nil
}
