// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"github.com/facebookincubator/ent/dialect/sql"
	"github.com/sut63/team17/app/ent/faculty"
)

// Faculty is the model entity for the Faculty schema.
type Faculty struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Faculty holds the value of the "faculty" field.
	Faculty string `json:"faculty,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the FacultyQuery when eager-loading is set.
	Edges FacultyEdges `json:"edges"`
}

// FacultyEdges holds the relations/edges for other nodes in the graph.
type FacultyEdges struct {
	// FacuCour holds the value of the facu_cour edge.
	FacuCour []*Course
	// FacuProf holds the value of the facu_prof edge.
	FacuProf []*Professor
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// FacuCourOrErr returns the FacuCour value or an error if the edge
// was not loaded in eager-loading.
func (e FacultyEdges) FacuCourOrErr() ([]*Course, error) {
	if e.loadedTypes[0] {
		return e.FacuCour, nil
	}
	return nil, &NotLoadedError{edge: "facu_cour"}
}

// FacuProfOrErr returns the FacuProf value or an error if the edge
// was not loaded in eager-loading.
func (e FacultyEdges) FacuProfOrErr() ([]*Professor, error) {
	if e.loadedTypes[1] {
		return e.FacuProf, nil
	}
	return nil, &NotLoadedError{edge: "facu_prof"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Faculty) scanValues() []interface{} {
	return []interface{}{
		&sql.NullInt64{},  // id
		&sql.NullString{}, // faculty
	}
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Faculty fields.
func (f *Faculty) assignValues(values ...interface{}) error {
	if m, n := len(values), len(faculty.Columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	value, ok := values[0].(*sql.NullInt64)
	if !ok {
		return fmt.Errorf("unexpected type %T for field id", value)
	}
	f.ID = int(value.Int64)
	values = values[1:]
	if value, ok := values[0].(*sql.NullString); !ok {
		return fmt.Errorf("unexpected type %T for field faculty", values[0])
	} else if value.Valid {
		f.Faculty = value.String
	}
	return nil
}

// QueryFacuCour queries the facu_cour edge of the Faculty.
func (f *Faculty) QueryFacuCour() *CourseQuery {
	return (&FacultyClient{config: f.config}).QueryFacuCour(f)
}

// QueryFacuProf queries the facu_prof edge of the Faculty.
func (f *Faculty) QueryFacuProf() *ProfessorQuery {
	return (&FacultyClient{config: f.config}).QueryFacuProf(f)
}

// Update returns a builder for updating this Faculty.
// Note that, you need to call Faculty.Unwrap() before calling this method, if this Faculty
// was returned from a transaction, and the transaction was committed or rolled back.
func (f *Faculty) Update() *FacultyUpdateOne {
	return (&FacultyClient{config: f.config}).UpdateOne(f)
}

// Unwrap unwraps the entity that was returned from a transaction after it was closed,
// so that all next queries will be executed through the driver which created the transaction.
func (f *Faculty) Unwrap() *Faculty {
	tx, ok := f.config.driver.(*txDriver)
	if !ok {
		panic("ent: Faculty is not a transactional entity")
	}
	f.config.driver = tx.drv
	return f
}

// String implements the fmt.Stringer.
func (f *Faculty) String() string {
	var builder strings.Builder
	builder.WriteString("Faculty(")
	builder.WriteString(fmt.Sprintf("id=%v", f.ID))
	builder.WriteString(", faculty=")
	builder.WriteString(f.Faculty)
	builder.WriteByte(')')
	return builder.String()
}

// Faculties is a parsable slice of Faculty.
type Faculties []*Faculty

func (f Faculties) config(cfg config) {
	for _i := range f {
		f[_i].config = cfg
	}
}
